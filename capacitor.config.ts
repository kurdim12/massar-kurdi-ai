import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'jo.masaar.intelligence',
  appName: 'MASAAR',
  webDir: 'out',
  android: {
    backgroundColor: '#0B1F3A',
  },
};

export default config;
